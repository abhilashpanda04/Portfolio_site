---
export interface Props {
  index: string;
  label: string;
}

const { index, label } = Astro.props;
---

<div class="section-label-accent font-mono text-caption text-accent uppercase tracking-[0.12em] mb-6 flex items-center">
  <span>{index} / {label}</span>
</div>
